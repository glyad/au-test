import { 
  Aurelia,
  FrameworkConfiguration,
  PLATFORM
} from "aurelia-framework";

//export const MODULE_NAME = 'dashboard';

export function configure(config: FrameworkConfiguration) {
  //config.globalResources(['./dashboard']);
}
